# Contains the standard algoritms 
# Mahir Tuzcu - 11070978

def random(Grid: object, :component: object):
    """random algorithm"""

    # TODO
    # select random house to connect to
    random_house, location = random.choice(list(Grid.batteries.items()))
    
    # add that house to end connection of batery and house
    connect_components(random_house, )
